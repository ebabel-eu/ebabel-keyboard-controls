'use strict';

const {
  keyboardControls,
  updatePlayerPositionRotation,
} = require('./src/ebabel-keyboard-controls');

module.exports = {
  keyboardControls,
  updatePlayerPositionRotation,
};
